<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="v1-create-delivery-attempts" author="abhilash">

        <createTable tableName="delivery_attempts">

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"
                             primaryKeyName="pk_delivery_attempts"
                             nullable="false"/>
            </column>

            <column name="event_id" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>

            <column name="user_id" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>

            <column name="event_type" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="channel" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="attempt_number" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="message_id" type="VARCHAR(255)"/>

            <column name="error_message" type="TEXT"/>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <!-- Indexes -->
        <createIndex
                tableName="delivery_attempts"
                indexName="idx_event_id">
            <column name="event_id"/>
        </createIndex>

        <createIndex
                tableName="delivery_attempts"
                indexName="idx_user_id">
            <column name="user_id"/>
        </createIndex>

        <createIndex
                tableName="delivery_attempts"
                indexName="idx_channel">
            <column name="channel"/>
        </createIndex>

        <createIndex
                tableName="delivery_attempts"
                indexName="idx_status">
            <column name="status"/>
        </createIndex>

        <createIndex
                tableName="delivery_attempts"
                indexName="idx_created_at">
            <column name="created_at"/>
        </createIndex>

        <createIndex
                tableName="delivery_attempts"
                indexName="idx_event_channel">
            <column name="event_id"/>
            <column name="channel"/>
        </createIndex>

        <createIndex
                tableName="delivery_attempts"
                indexName="idx_event_attempt">
            <column name="event_id"/>
            <column name="attempt_number"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>