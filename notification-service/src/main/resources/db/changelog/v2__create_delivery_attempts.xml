<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="2-create-delivery-attempts" author="abhilash">

        <createTable tableName="delivery_attempts">

            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true"/>
            </column>

            <column name="event_id" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>

            <column name="user_id" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>

            <column name="event_type" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="channel" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>

            <column name="attempt_number" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="message_id" type="VARCHAR(255)"/>

            <column name="error_message" type="TEXT"/>

            <column name="created_at"
                    type="TIMESTAMP WITH TIME ZONE"
                    defaultValueComputed="CURRENT_TIMESTAMP"/>

            <column name="updated_at"
                    type="TIMESTAMP WITH TIME ZONE"
                    defaultValueComputed="CURRENT_TIMESTAMP"/>

        </createTable>

        <createIndex indexName="idx_event_id"
                     tableName="delivery_attempts">
            <column name="event_id"/>
        </createIndex>

        <createIndex indexName="idx_user_id"
                     tableName="delivery_attempts">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_channel"
                     tableName="delivery_attempts">
            <column name="channel"/>
        </createIndex>

        <createIndex indexName="idx_status"
                     tableName="delivery_attempts">
            <column name="status"/>
        </createIndex>

        <createIndex indexName="idx_created_at"
                     tableName="delivery_attempts">
            <column name="created_at"/>
        </createIndex>

        <createIndex indexName="idx_delivery_event_channel"
                     tableName="delivery_attempts">

            <column name="event_id"/>
            <column name="channel"/>

        </createIndex>

    </changeSet>

</databaseChangeLog>