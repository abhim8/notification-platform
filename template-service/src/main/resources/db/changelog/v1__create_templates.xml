<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="v1-create-templates-table" author="abhilash">

        <createTable tableName="templates">

            <column name="id" type="VARCHAR(64)">
                <constraints primaryKey="true"
                             primaryKeyName="pk_templates"
                             nullable="false"/>
            </column>

            <column name="event_type" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>

            <column name="name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="subject" type="VARCHAR(500)"/>

            <column name="body" type="TEXT">
                <constraints nullable="false"/>
            </column>

            <column name="version" type="INTEGER">
                <constraints nullable="false"/>
            </column>

            <column name="active" type="BOOLEAN">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <!-- Indexes -->

        <createIndex
                tableName="templates"
                indexName="idx_template_event_type">
            <column name="event_type"/>
        </createIndex>

        <createIndex
                tableName="templates"
                indexName="idx_template_active">
            <column name="active"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>